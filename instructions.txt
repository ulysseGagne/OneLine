The “split edge” logic is broken. When i use the *split* tool in the HTML editor, everything is *displayed* properly. But if i export the edits, start from a fresh HTML template and re-import the SAME edits, there is a lot of weird stuff going on. These same bugs also appear in the debugging gpx export created at the beginning of the “solve.py” script.  Let’s say i split a parent edge into two child edges. Here’s what supposed to happen: the parent is deleted, and two edges are created for the two children. 
symptoms:{
 1- sometime after splitting a print node into two child node, a chid node has vanished when i re-import the file.  2- other times, there is a duplicated child edge that does not follow the curvature of the road.  }  a likely diagnostic: {  after careful observations, it looks like the problem is alway the same. The first child edge is created as intended. The second child edge has two problems: a- it is on the same side of the split as its sibling, and it does not follow the shape of the road. in other words, if the new node is the center of the parent edge, both children are on the same side of the center. & one follows the road, but the other does not. Obviously, they should both follow the road & be on separate sides of the center. The issue likely has to do with the fact that we sometimes have to *reverse* some edges so they are all connected in the right order. maybe we only reverse one of the child edge properly? please find out what the problem it.  }  examples: {  the segment between node 112 and 113 has been split by node new_135. logically, there are supposed to be new edges 112-new_135 and new_135-113. it looks like both new edges are new_135-113. One edge follows the road, one does not.  Same with 274-new_133-275, 285-new_50-349, 94-new_131-1496, among others.  }